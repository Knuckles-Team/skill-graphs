## v2.9.0 (2024-09-05)[¶](https://docs.pydantic.dev/latest/changelog/#v290-2024-09-05)
The code released in v2.9.0 is practically identical to that of v2.9.0b2.
### What's Changed[¶](https://docs.pydantic.dev/latest/changelog/#whats-changed_27)
#### Packaging[¶](https://docs.pydantic.dev/latest/changelog/#packaging_13)
  * Bump `ruff` to `v0.5.0` and `pyright` to `v1.1.369` by
  * Bump `pydantic-extra-types` to `v2.9.0` by
  * Support compatibility with `pdm v2.18.1` by
  * Bump `v1` version stub to `v1.10.18` by
  * Bump `pydantic-core` to `v2.23.2` by


#### New Features[¶](https://docs.pydantic.dev/latest/changelog/#new-features_8)
  * Add support for `ZoneInfo` by
  * Add `Config.val_json_bytes` by
  * Add DSN for Snowflake by
  * Support `complex` number by
  * Add support for `annotated_types.Not` by
  * Allow `WithJsonSchema` to inject `$ref`s w/ `http` or `https` links by
  * Allow validators to customize validation JSON schema by
  * Support parametrized `PathLike` types by
  * Add tagged union serializer that attempts to use `str` or `callable` discriminators to select the correct serializer by


#### Changes[¶](https://docs.pydantic.dev/latest/changelog/#changes_6)
  * Breaking Change: Merge `dict` type `json_schema_extra` by
    * For more info (how to replicate old behavior) on this change, see [here](https://docs.pydantic.dev/dev/concepts/json_schema/#merging-json_schema_extra)
  * Refactor annotation injection for known (often generic) types by
  * Move annotation compatibility errors to validation phase by
  * Improve runtime errors for string constraints like `pattern` for incompatible types by
  * Remove `'allOf'` JSON schema workarounds by
  * Remove `typed_dict_cls` data from `CoreMetadata` by
  * Deprecate passing a dict to the `Examples` class by
  * Remove `initial_metadata` from internal metadata construct by
  * Use `re.Pattern.search` instead of `re.Pattern.match` for consistency with `rust` behavior by
  * Show value of wrongly typed data in `pydantic-core` serialization warning by
  * Breaking Change: in `pydantic-core`, change `metadata` type hint in core schemas from `Any` -> `Dict[str, Any] | None` by
  * Raise helpful warning when `self` isn't returned from model validator by


#### Performance[¶](https://docs.pydantic.dev/latest/changelog/#performance_4)
  * Initial start at improving import times for modules, using caching primarily by
  * Using cached internal import for `BaseModel` by
  * Simplify internal generics logic - remove generator overhead by
  * Remove default module globals from types namespace by
  * Performance boost: skip caching parent namespaces in most cases by
  * Update ns stack with already copied ns by


##### Minor Internal Improvements[¶](https://docs.pydantic.dev/latest/changelog/#minor-internal-improvements)
  * ⚡️ Speed up `multiple_of_validator()` by 31% in `pydantic/_internal/_validators.py` by
  * ⚡️ Speed up `ModelPrivateAttr.__set_name__()` by 18% in `pydantic/fields.py` by
  * ⚡️ Speed up `dataclass()` by 7% in `pydantic/dataclasses.py` by
  * ⚡️ Speed up function `_field_name_for_signature` by 37% in `pydantic/_internal/_signature.py` by
  * ⚡️ Speed up method `GenerateSchema._unpack_refs_defs` by 26% in `pydantic/_internal/_generate_schema.py` by
  * ⚡️ Speed up function `apply_each_item_validators` by 100% in `pydantic/_internal/_generate_schema.py` by
  * ⚡️ Speed up method `ConfigWrapper.core_config` by 28% in `pydantic/_internal/_config.py` by


#### Fixes[¶](https://docs.pydantic.dev/latest/changelog/#fixes_26)
  * Respect `use_enum_values` on `Literal` types by
  * Prevent type error for exotic `BaseModel/RootModel` inheritance by
  * Fix typing issue with field_validator-decorated methods by
  * Replace `str` type annotation with `Any` in validator factories in documentation on validators by
  * Fix `ComputedFieldInfo.wrapped_property` pointer when a property setter is assigned by
  * Fix recursive typing of `main.IncEnx` by
  * Allow usage of `type[Annotated[...]]` by
  * `mypy` plugin: handle frozen fields on a per-field basis by
  * Fix typo in `invalid-annotated-type` error code by
  * Simplify schema generation for `uuid`, `url`, and `ip` types by
  * Move `date` schemas to `_generate_schema.py` by
  * Move `decimal.Decimal` validation to `_generate_schema.py` by
  * Simplify IP address schema in `_std_types_schema.py` by
  * Fix type annotations for some potentially generic `GenerateSchema.match_type` options by
  * Add class name to "has conflict" warnings by
  * Fix `dataclass` ignoring `default_factory` passed in Annotated by
  * Fix `Sequence` ignoring `discriminator` by
  * Fix typing for `IPvAnyAddress` and `IPvAnyInterface` by
  * Fix false positives on v1 models in `mypy` plugin for `from_orm` check requiring from_attributes=True config by
  * Apply `strict=True` to `__init__` in `mypy` plugin by
  * Refactor application of `deque` annotations by
  * Raise a better user error when failing to evaluate a forward reference by
  * Fix evaluation of `__pydantic_extra__` annotation in specific circumstances by
  * Fix `frozen` enforcement for `dataclasses` by
  * Remove logic to handle unused `__get_pydantic_core_schema__` signature by
  * Use `is_annotated` consistently by
  * Fix `PydanticDeprecatedSince26` typo by
  * Improve `pyright` tests, refactor model decorators signatures by
  * Fix `ip` serialization logic by
  * Warn when frozen defined twice for `dataclasses` by
  * Do not compute JSON Schema default when plain serializers are used with `when_used` set to `'json-unless-none'` and the default value is `None` by
  * Fix `ImportString` special cases by
  * Blacklist default globals to support exotic user code with `__` prefixed annotations by
  * Handle `nullable` schemas with `serialization` schema available during JSON Schema generation by
  * Reorganize `BaseModel` annotations by
  * Fix core schema simplification when serialization schemas are involved in specific scenarios by
  * Add support for stringified annotations when using `PrivateAttr` with `Annotated` by
  * Fix JSON Schema `number` type for literal and enum schemas by
  * Fix JSON Schema generation of fields with plain validators in serialization mode by
  * Fix invalid JSON Schemas being generated for functions in certain scenarios by
  * Make sure generated JSON Schemas are valid in tests by
  * Fix key error with custom serializer by
  * Add 'wss' for allowed schemes in NatsDsn by
  * Fix `Mapping` and `MutableMapping` annotations to use mapping schema instead of dict schema by
  * Fix JSON Schema generation for constrained dates by
  * Fix discriminated union bug regression when using enums by
  * Fix `field_serializer` with computed field when using `*` by
  * Try each option in `Union` serializer before inference by
  * Fix `float` serialization behavior in `strict` mode by
  * Introduce `exactness` into Decimal validation logic to improve union validation behavior by
  * Fix new warnings assertions to use `pytest.warns()` by
  * Fix a crash when cleaning the namespace in `ModelMetaclass` by
  * Fix parent namespace issue with model rebuilds by
  * Remove defaults filter for namespace by
  * Use identity instead of equality after validating model in `__init__` by
  * Support `BigInt` serialization for `int` subclasses by
  * Support signature for wrap validators without `info` by
  * Ensure `__pydantic_complete__` is set when rebuilding `dataclasses` by
  * Respect `schema_generator` config value in `TypeAdapter` by


### New Contributors[¶](https://docs.pydantic.dev/latest/changelog/#new-contributors_12)
####  `pydantic`[¶](https://docs.pydantic.dev/latest/changelog/#pydantic)
####  `pydantic-core`[¶](https://docs.pydantic.dev/latest/changelog/#pydantic-core)

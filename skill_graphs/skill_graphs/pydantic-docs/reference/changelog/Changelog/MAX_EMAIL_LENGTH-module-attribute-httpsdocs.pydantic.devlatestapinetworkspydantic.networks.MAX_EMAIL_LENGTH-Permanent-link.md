##  MAX_EMAIL_LENGTH `module-attribute` [¶](https://docs.pydantic.dev/latest/api/networks/#pydantic.networks.MAX_EMAIL_LENGTH "Permanent link")
```
MAX_EMAIL_LENGTH = 2048

```

Maximum length for an email. A somewhat arbitrary but very generous number compared to what is allowed by most implementations.

##  [Checking the version](https://vercel.com/docs/cli#checking-the-version)[](https://vercel.com/docs/cli#checking-the-version)
The `--version` option can be used to verify the version of Vercel CLI currently being used.
terminal
```
vercel --version
```

Using the `vercel` command with the `--version` option.

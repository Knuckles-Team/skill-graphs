## 0.93.0 (2023-03-07)[¶](https://fastapi.tiangolo.com/release-notes/#0930-2023-03-07)
### Features[¶](https://fastapi.tiangolo.com/release-notes/#features_30)
  * ✨ Add support for `lifespan` async context managers (superseding `startup` and `shutdown` events). Initial PR


Now, instead of using independent `startup` and `shutdown` events, you can define that logic in a single function with `yield` decorated with `@asynccontextmanager` (an async context manager).
For example:
```
from contextlib import asynccontextmanager

from fastapi import FastAPI


def fake_answer_to_everything_ml_model(x: float):
    return x * 42


ml_models = {}


@asynccontextmanager
async def lifespan(app: FastAPI):
    # Load the ML model
    ml_models["answer_to_everything"] = fake_answer_to_everything_ml_model
    yield
    # Clean up the ML models and release the resources
    ml_models.clear()


app = FastAPI(lifespan=lifespan)


@app.get("/predict")
async def predict(x: float):
    result = ml_models["answer_to_everything"](x)
    return {"result": result}

```

**Note** : This is the recommended way going forward, instead of using `startup` and `shutdown` events.
Read more about it in the new docs: [Advanced User Guide: Lifespan Events](https://fastapi.tiangolo.com/advanced/events/).
### Docs[¶](https://fastapi.tiangolo.com/release-notes/#docs_67)
  * ✏ Fix formatting in `docs/en/docs/tutorial/metadata.md` for `ReDoc`. PR


### Translations[¶](https://fastapi.tiangolo.com/release-notes/#translations_59)
  * 🌐 Tamil translations - initial setup. PR
  * 🌐 Add French translation for `docs/fr/docs/advanced/path-operation-advanced-configuration.md`. PR
  * 🌐 Add French translation for `docs/tutorial/debugging.md`. PR
  * 🌐 Initiate Armenian translation setup. PR
  * 🌐 Add French translation for `deployment/manually.md`. PR


### Internal[¶](https://fastapi.tiangolo.com/release-notes/#internal_88)
  * 👷 Update translation bot messages. PR
  * 👷 Update translations bot to use Discussions, and notify when a PR is done. PR
  * 🔧 Update sponsors-badges. PR
  * 👥 Update FastAPI People. PR
  * 🔊 Log GraphQL errors in FastAPI People, because it returns 200, with a payload with an error. PR
  * 💚 Fix/workaround GitHub Actions in Docker with git for FastAPI People. PR
  * ♻️ Refactor FastAPI Experts to use only discussions now that questions are migrated. PR
  * ⬆️ Upgrade analytics. PR
  * ⬆️ Upgrade and re-enable installing Typer-CLI. PR

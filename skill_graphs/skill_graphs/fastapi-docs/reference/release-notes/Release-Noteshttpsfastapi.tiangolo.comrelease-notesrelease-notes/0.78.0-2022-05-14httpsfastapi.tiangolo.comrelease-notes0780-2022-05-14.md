## 0.78.0 (2022-05-14)[¶](https://fastapi.tiangolo.com/release-notes/#0780-2022-05-14)
### Features[¶](https://fastapi.tiangolo.com/release-notes/#features_38)
  * ✨ Add support for omitting `...` as default value when declaring required parameters with:
  * `Path()`
  * `Query()`
  * `Header()`
  * `Cookie()`
  * `Body()`
  * `Form()`
  * `File()`


New docs at [Tutorial - Query Parameters and String Validations - Make it required](https://fastapi.tiangolo.com/tutorial/query-params-str-validations/#make-it-required). PR
Up to now, declaring a required parameter while adding additional validation or metadata needed using `...` (Ellipsis).
For example:
```
from fastapi import Cookie, FastAPI, Header, Path, Query

app = FastAPI()


@app.get("/items/{item_id}")
def main(
    item_id: int = Path(default=..., gt=0),
    query: str = Query(default=..., max_length=10),
    session: str = Cookie(default=..., min_length=3),
    x_trace: str = Header(default=..., title="Tracing header"),
):
    return {"message": "Hello World"}

```

...all these parameters are required because the default value is `...` (Ellipsis).
But now it's possible and supported to just omit the default value, as would be done with Pydantic fields, and the parameters would still be required.
✨ For example, this is now supported:
```
from fastapi import Cookie, FastAPI, Header, Path, Query

app = FastAPI()


@app.get("/items/{item_id}")
def main(
    item_id: int = Path(gt=0),
    query: str = Query(max_length=10),
    session: str = Cookie(min_length=3),
    x_trace: str = Header(title="Tracing header"),
):
    return {"message": "Hello World"}

```

To declare parameters as optional (not required), you can set a default value as always, for example using `None`:
```
from typing import Union
from fastapi import Cookie, FastAPI, Header, Path, Query

app = FastAPI()


@app.get("/items/{item_id}")
def main(
    item_id: int = Path(gt=0),
    query: Union[str, None] = Query(default=None, max_length=10),
    session: Union[str, None] = Cookie(default=None, min_length=3),
    x_trace: Union[str, None] = Header(default=None, title="Tracing header"),
):
    return {"message": "Hello World"}

```

### Docs[¶](https://fastapi.tiangolo.com/release-notes/#docs_81)
  * 📝 Add docs recommending `Union` over `Optional` and migrate source examples. New docs at [Python Types Intro - Using `Union` or `Optional`](https://fastapi.tiangolo.com/python-types/#using-union-or-optional). PR
  * 🎨 Fix default value as set in tutorial for Path Operations Advanced Configurations. PR
  * 📝 Add documentation for redefined path operations. PR
  * 📝 Updates links for Celery documentation. PR
  * ✏ Fix example code with sets in tutorial for body nested models. PR
  * ✏ Fix links to Pydantic docs. PR
  * 📝 Update docs about Swagger UI self-hosting with newer source links. PR
  * 📝 Add link to external article: Building the Poll App From Django Tutorial With FastAPI And React. PR
  * 📝 Add OpenAPI warning to "Body - Fields" docs with extra schema extensions. PR


### Translations[¶](https://fastapi.tiangolo.com/release-notes/#translations_73)
  * 🌐 Fix code examples in Japanese translation for `docs/ja/docs/tutorial/testing.md`. PR


### Internal[¶](https://fastapi.tiangolo.com/release-notes/#internal_103)
  * ♻ Refactor dict value extraction to minimize key lookups `fastapi/utils.py`. PR
  * ✅ Add tests for required nonable parameters and body fields. PR
  * 👷 Fix installing Material for MkDocs Insiders in CI. PR
  * 👷 Add pre-commit CI instead of custom GitHub Action. PR
  * 👷 Add pre-commit GitHub Action workflow. PR
  * 📝 Add dark mode auto switch to docs based on OS preference. PR
  * 🔥 Remove un-used old pending tests, already covered in other places. PR
  * 🔧 Add Python formatting hooks to pre-commit. PR
  * 🔧 Add pre-commit with first config and first formatting pass. PR
  * 👷 Disable CI installing Material for MkDocs in forks. PR

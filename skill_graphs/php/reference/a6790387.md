## [Available Validation Rules](https://laravel.com/docs/12.x/validation#available-validation-rules)
Below is a list of all available validation rules and their function:
#### Booleans
[Accepted](https://laravel.com/docs/12.x/validation#rule-accepted) [Accepted If](https://laravel.com/docs/12.x/validation#rule-accepted-if) [Boolean](https://laravel.com/docs/12.x/validation#rule-boolean) [Declined](https://laravel.com/docs/12.x/validation#rule-declined) [Declined If](https://laravel.com/docs/12.x/validation#rule-declined-if)
#### Strings
[Active URL](https://laravel.com/docs/12.x/validation#rule-active-url) [Alpha](https://laravel.com/docs/12.x/validation#rule-alpha) [Alpha Dash](https://laravel.com/docs/12.x/validation#rule-alpha-dash) [Alpha Numeric](https://laravel.com/docs/12.x/validation#rule-alpha-num) [Ascii](https://laravel.com/docs/12.x/validation#rule-ascii) [Confirmed](https://laravel.com/docs/12.x/validation#rule-confirmed) [Current Password](https://laravel.com/docs/12.x/validation#rule-current-password) [Different](https://laravel.com/docs/12.x/validation#rule-different) [Doesnt Start With](https://laravel.com/docs/12.x/validation#rule-doesnt-start-with) [Doesnt End With](https://laravel.com/docs/12.x/validation#rule-doesnt-end-with) [Email](https://laravel.com/docs/12.x/validation#rule-email) [Ends With](https://laravel.com/docs/12.x/validation#rule-ends-with) [Enum](https://laravel.com/docs/12.x/validation#rule-enum) [Hex Color](https://laravel.com/docs/12.x/validation#rule-hex-color) [In](https://laravel.com/docs/12.x/validation#rule-in) [IP Address](https://laravel.com/docs/12.x/validation#rule-ip) [JSON](https://laravel.com/docs/12.x/validation#rule-json) [Lowercase](https://laravel.com/docs/12.x/validation#rule-lowercase) [MAC Address](https://laravel.com/docs/12.x/validation#rule-mac) [Max](https://laravel.com/docs/12.x/validation#rule-max) [Min](https://laravel.com/docs/12.x/validation#rule-min) [Not In](https://laravel.com/docs/12.x/validation#rule-not-in) [Regular Expression](https://laravel.com/docs/12.x/validation#rule-regex) [Not Regular Expression](https://laravel.com/docs/12.x/validation#rule-not-regex) [Same](https://laravel.com/docs/12.x/validation#rule-same) [Size](https://laravel.com/docs/12.x/validation#rule-size) [Starts With](https://laravel.com/docs/12.x/validation#rule-starts-with) [String](https://laravel.com/docs/12.x/validation#rule-string) [Uppercase](https://laravel.com/docs/12.x/validation#rule-uppercase) [URL](https://laravel.com/docs/12.x/validation#rule-url) [ULID](https://laravel.com/docs/12.x/validation#rule-ulid) [UUID](https://laravel.com/docs/12.x/validation#rule-uuid)
#### Numbers
[Between](https://laravel.com/docs/12.x/validation#rule-between) [Decimal](https://laravel.com/docs/12.x/validation#rule-decimal) [Different](https://laravel.com/docs/12.x/validation#rule-different) [Digits](https://laravel.com/docs/12.x/validation#rule-digits) [Digits Between](https://laravel.com/docs/12.x/validation#rule-digits-between) [Greater Than](https://laravel.com/docs/12.x/validation#rule-gt) [Greater Than Or Equal](https://laravel.com/docs/12.x/validation#rule-gte) [Integer](https://laravel.com/docs/12.x/validation#rule-integer) [Less Than](https://laravel.com/docs/12.x/validation#rule-lt) [Less Than Or Equal](https://laravel.com/docs/12.x/validation#rule-lte) [Max](https://laravel.com/docs/12.x/validation#rule-max) [Max Digits](https://laravel.com/docs/12.x/validation#rule-max-digits) [Min](https://laravel.com/docs/12.x/validation#rule-min) [Min Digits](https://laravel.com/docs/12.x/validation#rule-min-digits) [Multiple Of](https://laravel.com/docs/12.x/validation#rule-multiple-of) [Numeric](https://laravel.com/docs/12.x/validation#rule-numeric) [Same](https://laravel.com/docs/12.x/validation#rule-same) [Size](https://laravel.com/docs/12.x/validation#rule-size)
#### Arrays
[Array](https://laravel.com/docs/12.x/validation#rule-array) [Between](https://laravel.com/docs/12.x/validation#rule-between) [Contains](https://laravel.com/docs/12.x/validation#rule-contains) [Doesnt Contain](https://laravel.com/docs/12.x/validation#rule-doesnt-contain) [Distinct](https://laravel.com/docs/12.x/validation#rule-distinct) [In Array](https://laravel.com/docs/12.x/validation#rule-in-array) [In Array Keys](https://laravel.com/docs/12.x/validation#rule-in-array-keys) [List](https://laravel.com/docs/12.x/validation#rule-list) [Max](https://laravel.com/docs/12.x/validation#rule-max) [Min](https://laravel.com/docs/12.x/validation#rule-min) [Size](https://laravel.com/docs/12.x/validation#rule-size)
#### Dates
[After](https://laravel.com/docs/12.x/validation#rule-after) [After Or Equal](https://laravel.com/docs/12.x/validation#rule-after-or-equal) [Before](https://laravel.com/docs/12.x/validation#rule-before) [Before Or Equal](https://laravel.com/docs/12.x/validation#rule-before-or-equal) [Date](https://laravel.com/docs/12.x/validation#rule-date) [Date Equals](https://laravel.com/docs/12.x/validation#rule-date-equals) [Date Format](https://laravel.com/docs/12.x/validation#rule-date-format) [Different](https://laravel.com/docs/12.x/validation#rule-different) [Timezone](https://laravel.com/docs/12.x/validation#rule-timezone)
#### Files
[Between](https://laravel.com/docs/12.x/validation#rule-between) [Dimensions](https://laravel.com/docs/12.x/validation#rule-dimensions) [Encoding](https://laravel.com/docs/12.x/validation#rule-encoding) [Extensions](https://laravel.com/docs/12.x/validation#rule-extensions) [File](https://laravel.com/docs/12.x/validation#rule-file) [Image](https://laravel.com/docs/12.x/validation#rule-image) [Max](https://laravel.com/docs/12.x/validation#rule-max) [MIME Types](https://laravel.com/docs/12.x/validation#rule-mimetypes) [MIME Type By File Extension](https://laravel.com/docs/12.x/validation#rule-mimes) [Size](https://laravel.com/docs/12.x/validation#rule-size)
#### Database
[Exists](https://laravel.com/docs/12.x/validation#rule-exists) [Unique](https://laravel.com/docs/12.x/validation#rule-unique)
#### Utilities
[Any Of](https://laravel.com/docs/12.x/validation#rule-anyof) [Bail](https://laravel.com/docs/12.x/validation#rule-bail) [Exclude](https://laravel.com/docs/12.x/validation#rule-exclude) [Exclude If](https://laravel.com/docs/12.x/validation#rule-exclude-if) [Exclude Unless](https://laravel.com/docs/12.x/validation#rule-exclude-unless) [Exclude With](https://laravel.com/docs/12.x/validation#rule-exclude-with) [Exclude Without](https://laravel.com/docs/12.x/validation#rule-exclude-without) [Filled](https://laravel.com/docs/12.x/validation#rule-filled) [Missing](https://laravel.com/docs/12.x/validation#rule-missing) [Missing If](https://laravel.com/docs/12.x/validation#rule-missing-if) [Missing Unless](https://laravel.com/docs/12.x/validation#rule-missing-unless) [Missing With](https://laravel.com/docs/12.x/validation#rule-missing-with) [Missing With All](https://laravel.com/docs/12.x/validation#rule-missing-with-all) [Nullable](https://laravel.com/docs/12.x/validation#rule-nullable) [Present](https://laravel.com/docs/12.x/validation#rule-present) [Present If](https://laravel.com/docs/12.x/validation#rule-present-if) [Present Unless](https://laravel.com/docs/12.x/validation#rule-present-unless) [Present With](https://laravel.com/docs/12.x/validation#rule-present-with) [Present With All](https://laravel.com/docs/12.x/validation#rule-present-with-all) [Prohibited](https://laravel.com/docs/12.x/validation#rule-prohibited) [Prohibited If](https://laravel.com/docs/12.x/validation#rule-prohibited-if) [Prohibited If Accepted](https://laravel.com/docs/12.x/validation#rule-prohibited-if-accepted) [Prohibited If Declined](https://laravel.com/docs/12.x/validation#rule-prohibited-if-declined) [Prohibited Unless](https://laravel.com/docs/12.x/validation#rule-prohibited-unless) [Prohibits](https://laravel.com/docs/12.x/validation#rule-prohibits) [Required](https://laravel.com/docs/12.x/validation#rule-required) [Required If](https://laravel.com/docs/12.x/validation#rule-required-if) [Required If Accepted](https://laravel.com/docs/12.x/validation#rule-required-if-accepted) [Required If Declined](https://laravel.com/docs/12.x/validation#rule-required-if-declined) [Required Unless](https://laravel.com/docs/12.x/validation#rule-required-unless) [Required With](https://laravel.com/docs/12.x/validation#rule-required-with) [Required With All](https://laravel.com/docs/12.x/validation#rule-required-with-all) [Required Without](https://laravel.com/docs/12.x/validation#rule-required-without) [Required Without All](https://laravel.com/docs/12.x/validation#rule-required-without-all) [Required Array Keys](https://laravel.com/docs/12.x/validation#rule-required-array-keys) [Sometimes](https://laravel.com/docs/12.x/validation#validating-when-present)
#### [accepted](https://laravel.com/docs/12.x/validation#rule-accepted)
The field under validation must be `"yes"`, `"on"`, `1`, `"1"`, `true`, or `"true"`. This is useful for validating "Terms of Service" acceptance or similar fields.
#### [accepted_if:anotherfield,value,...](https://laravel.com/docs/12.x/validation#rule-accepted-if)
The field under validation must be `"yes"`, `"on"`, `1`, `"1"`, `true`, or `"true"` if another field under validation is equal to a specified value. This is useful for validating "Terms of Service" acceptance or similar fields.
#### [active_url](https://laravel.com/docs/12.x/validation#rule-active-url)
The field under validation must have a valid A or AAAA record according to the `dns_get_record` PHP function. The hostname of the provided URL is extracted using the `parse_url` PHP function before being passed to `dns_get_record`.
#### [after:_date_](https://laravel.com/docs/12.x/validation#rule-after)
The field under validation must be a value after a given date. The dates will be passed into the `strtotime` PHP function in order to be converted to a valid `DateTime` instance:
```


1'start_date' => 'required|date|after:tomorrow'




'start_date' => 'required|date|after:tomorrow'

```

Instead of passing a date string to be evaluated by `strtotime`, you may specify another field to compare against the date:
```


1'finish_date' => 'required|date|after:start_date'




'finish_date' => 'required|date|after:start_date'

```

For convenience, date-based rules may be constructed using the fluent `date` rule builder:
```


1use Illuminate\Validation\Rule;




2 



3'start_date' => [




4    'required',




5    Rule::date()->after(today()->addDays(7)),




6],




use Illuminate\Validation\Rule;

'start_date' => [
    'required',
    Rule::date()->after(today()->addDays(7)),
],

```

The `afterToday` and `todayOrAfter` methods may be used to fluently express the date and must be after today, or today or after, respectively:
```


1'start_date' => [




2    'required',




3    Rule::date()->afterToday(),




4],




'start_date' => [
    'required',
    Rule::date()->afterToday(),
],

```

#### [after_or_equal:_date_](https://laravel.com/docs/12.x/validation#rule-after-or-equal)
The field under validation must be a value after or equal to the given date. For more information, see the [after](https://laravel.com/docs/12.x/validation#rule-after) rule.
For convenience, date-based rules may be constructed using the fluent `date` rule builder:
```


1use Illuminate\Validation\Rule;




2 



3'start_date' => [




4    'required',




5    Rule::date()->afterOrEqual(today()->addDays(7)),




6],




use Illuminate\Validation\Rule;

'start_date' => [
    'required',
    Rule::date()->afterOrEqual(today()->addDays(7)),
],

```

#### [anyOf](https://laravel.com/docs/12.x/validation#rule-anyof)
The `Rule::anyOf` validation rule allows you to specify that the field under validation must satisfy any of the given validation rulesets. For example, the following rule will validate that the `username` field is either an email address or an alphanumeric string (including dashes) that is at least 6 characters long:
```


1use Illuminate\Validation\Rule;




2 



3'username' => [




4    'required',




5    Rule::anyOf([




6        ['string', 'email'],




7        ['string', 'alpha_dash', 'min:6'],




8    ]),




9],




use Illuminate\Validation\Rule;

'username' => [
    'required',
    Rule::anyOf([
        ['string', 'email'],
        ['string', 'alpha_dash', 'min:6'],
    ]),
],

```

#### [alpha](https://laravel.com/docs/12.x/validation#rule-alpha)
The field under validation must be entirely Unicode alphabetic characters contained in
To restrict this validation rule to characters in the ASCII range (`a-z` and `A-Z`), you may provide the `ascii` option to the validation rule:
```


1'username' => 'alpha:ascii',




'username' => 'alpha:ascii',

```

#### [alpha_dash](https://laravel.com/docs/12.x/validation#rule-alpha-dash)
The field under validation must be entirely Unicode alphanumeric characters contained in `-`) and ASCII underscores (`_`).
To restrict this validation rule to characters in the ASCII range (`a-z`, `A-Z`, and `0-9`), you may provide the `ascii` option to the validation rule:
```


1'username' => 'alpha_dash:ascii',




'username' => 'alpha_dash:ascii',

```

#### [alpha_num](https://laravel.com/docs/12.x/validation#rule-alpha-num)
The field under validation must be entirely Unicode alphanumeric characters contained in
To restrict this validation rule to characters in the ASCII range (`a-z`, `A-Z`, and `0-9`), you may provide the `ascii` option to the validation rule:
```


1'username' => 'alpha_num:ascii',




'username' => 'alpha_num:ascii',

```

#### [array](https://laravel.com/docs/12.x/validation#rule-array)
The field under validation must be a PHP `array`.
When additional values are provided to the `array` rule, each key in the input array must be present within the list of values provided to the rule. In the following example, the `admin` key in the input array is invalid since it is not contained in the list of values provided to the `array` rule:
```


 1use Illuminate\Support\Facades\Validator;




 2 



 3$input = [




 4    'user' => [




 5        'name' => 'Taylor Otwell',




 6        'username' => 'taylorotwell',




 7        'admin' => true,




 8    ],




 9];




10 



11Validator::make($input, [




12    'user' => 'array:name,username',




13]);




use Illuminate\Support\Facades\Validator;

$input = [
    'user' => [
        'name' => 'Taylor Otwell',
        'username' => 'taylorotwell',
        'admin' => true,
    ],
];

Validator::make($input, [
    'user' => 'array:name,username',
]);

```

In general, you should always specify the array keys that are allowed to be present within your array.
#### [ascii](https://laravel.com/docs/12.x/validation#rule-ascii)
The field under validation must be entirely 7-bit ASCII characters.
#### [bail](https://laravel.com/docs/12.x/validation#rule-bail)
Stop running validation rules for the field after the first validation failure.
While the `bail` rule will only stop validating a specific field when it encounters a validation failure, the `stopOnFirstFailure` method will inform the validator that it should stop validating all attributes once a single validation failure has occurred:
```


1if ($validator->stopOnFirstFailure()->fails()) {




2    // ...




3}




if ($validator->stopOnFirstFailure()->fails()) {
    // ...
}

```

#### [before:_date_](https://laravel.com/docs/12.x/validation#rule-before)
The field under validation must be a value preceding the given date. The dates will be passed into the PHP `strtotime` function in order to be converted into a valid `DateTime` instance. In addition, like the [after](https://laravel.com/docs/12.x/validation#rule-after) rule, the name of another field under validation may be supplied as the value of `date`.
For convenience, date-based rules may also be constructed using the fluent `date` rule builder:
```


1use Illuminate\Validation\Rule;




2 



3'start_date' => [




4    'required',




5    Rule::date()->before(today()->subDays(7)),




6],




use Illuminate\Validation\Rule;

'start_date' => [
    'required',
    Rule::date()->before(today()->subDays(7)),
],

```

The `beforeToday` and `todayOrBefore` methods may be used to fluently express the date and must be before today, or today or before, respectively:
```


1'start_date' => [




2    'required',




3    Rule::date()->beforeToday(),




4],




'start_date' => [
    'required',
    Rule::date()->beforeToday(),
],

```

#### [before_or_equal:_date_](https://laravel.com/docs/12.x/validation#rule-before-or-equal)
The field under validation must be a value preceding or equal to the given date. The dates will be passed into the PHP `strtotime` function in order to be converted into a valid `DateTime` instance. In addition, like the [after](https://laravel.com/docs/12.x/validation#rule-after) rule, the name of another field under validation may be supplied as the value of `date`.
For convenience, date-based rules may also be constructed using the fluent `date` rule builder:
```


1use Illuminate\Validation\Rule;




2 



3'start_date' => [




4    'required',




5    Rule::date()->beforeOrEqual(today()->subDays(7)),




6],




use Illuminate\Validation\Rule;

'start_date' => [
    'required',
    Rule::date()->beforeOrEqual(today()->subDays(7)),
],

```

#### [between:_min_ ,_max_](https://laravel.com/docs/12.x/validation#rule-between)
The field under validation must have a size between the given _min_ and _max_ (inclusive). Strings, numerics, arrays, and files are evaluated in the same fashion as the [size](https://laravel.com/docs/12.x/validation#rule-size) rule.
#### [boolean](https://laravel.com/docs/12.x/validation#rule-boolean)
The field under validation must be able to be cast as a boolean. Accepted input are `true`, `false`, `1`, `0`, `"1"`, and `"0"`.
You may use the `strict` parameter to only consider the field valid if its value is `true` or `false`:
```


1'foo' => 'boolean:strict'




'foo' => 'boolean:strict'

```

#### [confirmed](https://laravel.com/docs/12.x/validation#rule-confirmed)
The field under validation must have a matching field of `{field}_confirmation`. For example, if the field under validation is `password`, a matching `password_confirmation` field must be present in the input.
You may also pass a custom confirmation field name. For example, `confirmed:repeat_username` will expect the field `repeat_username` to match the field under validation.
#### [contains:_foo_ ,_bar_ ,...](https://laravel.com/docs/12.x/validation#rule-contains)
The field under validation must be an array that contains all of the given parameter values. Since this rule often requires you to `implode` an array, the `Rule::contains` method may be used to fluently construct the rule:
```


 1use Illuminate\Support\Facades\Validator;




 2use Illuminate\Validation\Rule;




 3 



 4Validator::make($data, [




 5    'roles' => [




 6        'required',




 7        'array',




 8        Rule::contains(['admin', 'editor']),




 9    ],




10]);




use Illuminate\Support\Facades\Validator;
use Illuminate\Validation\Rule;

Validator::make($data, [
    'roles' => [
        'required',
        'array',
        Rule::contains(['admin', 'editor']),
    ],
]);

```

#### [doesnt_contain:_foo_ ,_bar_ ,...](https://laravel.com/docs/12.x/validation#rule-doesnt-contain)
The field under validation must be an array that does not contain any of the given parameter values. Since this rule often requires you to `implode` an array, the `Rule::doesntContain` method may be used to fluently construct the rule:
```


 1use Illuminate\Support\Facades\Validator;




 2use Illuminate\Validation\Rule;




 3 



 4Validator::make($data, [




 5    'roles' => [




 6        'required',




 7        'array',




 8        Rule::doesntContain(['admin', 'editor']),




 9    ],




10]);




use Illuminate\Support\Facades\Validator;
use Illuminate\Validation\Rule;

Validator::make($data, [
    'roles' => [
        'required',
        'array',
        Rule::doesntContain(['admin', 'editor']),
    ],
]);

```

#### [current_password](https://laravel.com/docs/12.x/validation#rule-current-password)
The field under validation must match the authenticated user's password. You may specify an [authentication guard](https://laravel.com/docs/12.x/authentication) using the rule's first parameter:
```


1'password' => 'current_password:api'




'password' => 'current_password:api'

```

#### [date](https://laravel.com/docs/12.x/validation#rule-date)
The field under validation must be a valid, non-relative date according to the `strtotime` PHP function.
#### [date_equals:_date_](https://laravel.com/docs/12.x/validation#rule-date-equals)
The field under validation must be equal to the given date. The dates will be passed into the PHP `strtotime` function in order to be converted into a valid `DateTime` instance.
#### [date_format:_format_ ,...](https://laravel.com/docs/12.x/validation#rule-date-format)
The field under validation must match one of the given _formats_. You should use **either** `date` or `date_format` when validating a field, not both. This validation rule supports all formats supported by PHP's
For convenience, date-based rules may be constructed using the fluent `date` rule builder:
```


1use Illuminate\Validation\Rule;




2 



3'start_date' => [




4    'required',




5    Rule::date()->format('Y-m-d'),




6],




use Illuminate\Validation\Rule;

'start_date' => [
    'required',
    Rule::date()->format('Y-m-d'),
],

```

#### [decimal:_min_ ,_max_](https://laravel.com/docs/12.x/validation#rule-decimal)
The field under validation must be numeric and must contain the specified number of decimal places:
```


1// Must have exactly two decimal places (9.99)...




2'price' => 'decimal:2'




3 



4// Must have between 2 and 4 decimal places...




5'price' => 'decimal:2,4'




// Must have exactly two decimal places (9.99)...
'price' => 'decimal:2'

// Must have between 2 and 4 decimal places...
'price' => 'decimal:2,4'

```

#### [declined](https://laravel.com/docs/12.x/validation#rule-declined)
The field under validation must be `"no"`, `"off"`, `0`, `"0"`, `false`, or `"false"`.
#### [declined_if:anotherfield,value,...](https://laravel.com/docs/12.x/validation#rule-declined-if)
The field under validation must be `"no"`, `"off"`, `0`, `"0"`, `false`, or `"false"` if another field under validation is equal to a specified value.
#### [different:_field_](https://laravel.com/docs/12.x/validation#rule-different)
The field under validation must have a different value than _field_.
#### [digits:_value_](https://laravel.com/docs/12.x/validation#rule-digits)
The integer under validation must have an exact length of _value_.
#### [digits_between:_min_ ,_max_](https://laravel.com/docs/12.x/validation#rule-digits-between)
The integer under validation must have a length between the given _min_ and _max_.
#### [dimensions](https://laravel.com/docs/12.x/validation#rule-dimensions)
The file under validation must be an image meeting the dimension constraints as specified by the rule's parameters:
```


1'avatar' => 'dimensions:min_width=100,min_height=200'




'avatar' => 'dimensions:min_width=100,min_height=200'

```

Available constraints are: _min_width_ , _max_width_ , _min_height_ , _max_height_ , _width_ , _height_ , _ratio_.
A _ratio_ constraint should be represented as width divided by height. This can be specified either by a fraction like `3/2` or a float like `1.5`:
```


1'avatar' => 'dimensions:ratio=3/2'




'avatar' => 'dimensions:ratio=3/2'

```

Since this rule requires several arguments, it is often more convenient to use the `Rule::dimensions` method to fluently construct the rule:
```


 1use Illuminate\Support\Facades\Validator;




 2use Illuminate\Validation\Rule;




 3 



 4Validator::make($data, [




 5    'avatar' => [




 6        'required',




 7        Rule::dimensions()




 8            ->maxWidth(1000)




 9            ->maxHeight(500)




10            ->ratio(3 / 2),




11    ],




12]);




use Illuminate\Support\Facades\Validator;
use Illuminate\Validation\Rule;

Validator::make($data, [
    'avatar' => [
        'required',
        Rule::dimensions()
            ->maxWidth(1000)
            ->maxHeight(500)
            ->ratio(3 / 2),
    ],
]);

```

#### [distinct](https://laravel.com/docs/12.x/validation#rule-distinct)
When validating arrays, the field under validation must not have any duplicate values:
```


1'foo.*.id' => 'distinct'




'foo.*.id' => 'distinct'

```

Distinct uses loose variable comparisons by default. To use strict comparisons, you may add the `strict` parameter to your validation rule definition:
```


1'foo.*.id' => 'distinct:strict'




'foo.*.id' => 'distinct:strict'

```

You may add `ignore_case` to the validation rule's arguments to make the rule ignore capitalization differences:
```


1'foo.*.id' => 'distinct:ignore_case'




'foo.*.id' => 'distinct:ignore_case'

```

#### [doesnt_start_with:_foo_ ,_bar_ ,...](https://laravel.com/docs/12.x/validation#rule-doesnt-start-with)
The field under validation must not start with one of the given values.
#### [doesnt_end_with:_foo_ ,_bar_ ,...](https://laravel.com/docs/12.x/validation#rule-doesnt-end-with)
The field under validation must not end with one of the given values.
#### [email](https://laravel.com/docs/12.x/validation#rule-email)
The field under validation must be formatted as an email address. This validation rule utilizes the `RFCValidation` validator is applied, but you can apply other validation styles as well:

## MCP
### Change JSON Response Formatting to Compact by Default
Pull request by
JSON responses are now compact by default, trimming payload size and making tool-to-tool communication faster and less noisy in logs.
## MCP
### Add Completion Support
Pull request by
Support was added for the
### Add Annotation Support on Resources
Pull request by
Three annotation types are now available for use with Resource classes: `audience`, `priority`, and `lastModified`. This adheres to the
### Add `structuredContent` and `outputSchema` Support
Pull request by
Support for output schemas and structured content was added to the MCP package, enabling better integration with ChatGPT Apps and AI clients that need parseable tool results.
## MCP
### Add `_meta` Support
Pull request by
The MCP package now fully supports the `_meta` field as documented in the
```


1public function handle(Request $request): Response




2{




3    return Response::text('The content generated by the tool.')




4        ->withMeta('x', 'y');




5}




public function handle(Request $request): Response
{
    return Response::text('The content generated by the tool.')
        ->withMeta('x', 'y');
}

```

## MCP
### First Official "Beta" Release of Laravel MCP Package
Laravel MCP allows you to rapidly build MCP servers for your Laravel applications. MCP servers allow AI clients to interact with your Laravel application through the

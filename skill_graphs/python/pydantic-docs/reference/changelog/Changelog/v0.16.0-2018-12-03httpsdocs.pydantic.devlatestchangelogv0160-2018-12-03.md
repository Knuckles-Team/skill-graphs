## v0.16.0 (2018-12-03)[¶](https://docs.pydantic.dev/latest/changelog/#v0160-2018-12-03)
  * **breaking change** : refactor schema generation to be compatible with JSON Schema and OpenAPI specs,
  * add `schema` to `schema` module to generate top-level schemas from base models,
  * add additional fields to `Schema` class to declare validation for `str` and numeric values,
  * rename `_schema` to `schema` on fields,
  * add `case_insensitive` option to `BaseSettings` `Config`,

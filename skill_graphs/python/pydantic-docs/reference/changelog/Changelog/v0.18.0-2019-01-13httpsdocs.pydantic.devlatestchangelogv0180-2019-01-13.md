## v0.18.0 (2019-01-13)[¶](https://docs.pydantic.dev/latest/changelog/#v0180-2019-01-13)
  * **breaking change** : don't call validators on keys of dictionaries,
  * Fix validators with `always=True` when the default is `None` or the type is optional, also prevent `whole` validators being called for sub-fields, fix
  * improve documentation for settings priority and allow it to be easily changed,
  * fix `ignore_extra=False` and `allow_population_by_alias=True`, fix
  * **breaking change** : Set `BaseConfig` attributes `min_anystr_length` and `max_anystr_length` to `None` by default, fix
  * add support for postponed annotations,

## v2.8.0 (2024-07-01)[¶](https://docs.pydantic.dev/latest/changelog/#v280-2024-07-01)
The code released in v2.8.0 is functionally identical to that of v2.8.0b1.
### What's Changed[¶](https://docs.pydantic.dev/latest/changelog/#whats-changed_30)
#### Packaging[¶](https://docs.pydantic.dev/latest/changelog/#packaging_15)
  * Update citation version automatically with new releases by
  * Bump pyright to `v1.1.367` and add type checking tests for pipeline API by
  * Update `pydantic.v1` stub to `v1.10.17` by
  * General package updates to prep for `v2.8.0b1` by
  * Bump `pydantic-core` to `v2.20.0` by
  * Add support for Python 3.13 by
  * Update `pdm` version used for `pdm.lock` to v2.16.1 by
  * Update to `ruff` `v0.4.8` by


#### New Features[¶](https://docs.pydantic.dev/latest/changelog/#new-features_9)
  * Experimental: support `defer_build` for `TypeAdapter` by
  * Implement `deprecated` field in json schema by
  * Experimental: Add pipeline API by
  * Add support for programmatic title generation by
  * Implement `fail_fast` feature by
  * Add `ser_json_inf_nan='strings'` mode to produce valid JSON by


#### Changes[¶](https://docs.pydantic.dev/latest/changelog/#changes_7)
  * Add warning when "alias" is set in ignored `Annotated` field by
  * Support serialization of some serializable defaults in JSON schema by
  * Relax type specification for `__validators__` values in `create_model` by
  * **Breaking Change:** Improve `smart` union matching logic by `smart` union matching logic [here](https://docs.pydantic.dev/dev/concepts/unions/#smart-mode). In some cases, if the old behavior is desired, you can switch to `left-to-right` mode and change the order of your `Union` members.


#### Performance[¶](https://docs.pydantic.dev/latest/changelog/#performance_5)
##### Internal Improvements[¶](https://docs.pydantic.dev/latest/changelog/#internal-improvements)
  * ⚡️ Speed up `_display_error_loc()` by 25% in `pydantic/v1/error_wrappers.py` by
  * ⚡️ Speed up `_get_all_json_refs()` by 34% in `pydantic/json_schema.py` by
  * ⚡️ Speed up `is_pydantic_dataclass()` by 41% in `pydantic/dataclasses.py` by
  * ⚡️ Speed up `to_snake()` by 27% in `pydantic/alias_generators.py` by
  * ⚡️ Speed up `unwrap_wrapped_function()` by 93% in `pydantic/_internal/_decorators.py` by


#### Fixes[¶](https://docs.pydantic.dev/latest/changelog/#fixes_29)
  * Replace `__spec__.parent` with `__package__` by
  * Fix Outputted Model JSON Schema for `Sequence` type by
  * Fix typing of `_frame_depth` by
  * Make `ImportString` json schema compatible by
  * Hide private attributes (`PrivateAttr`) from `__init__` signature in type checkers by
  * Make detection of `TypeVar` defaults robust to the CPython `PEP-696` implementation by
  * Fix usage of `PlainSerializer` with builtin types by
  * Add more robust custom validation examples by
  * Fix ignored `strict` specification for `StringConstraint(strict=False)` by
  * **Breaking Change:** Use PEP 570 syntax by
  * Use `Self` where possible by
  * Do not alter `RootModel.model_construct` signature in the `mypy` plugin by
  * Fixed type hint of `validation_context` by
  * Support context being passed to TypeAdapter's `dump_json`/`dump_python` by
  * Updates type signature for `Field()` constructor by
  * Improve builtin alias generators by
  * Fix typing of `TypeAdapter` by
  * Add fallback default value for private fields in `__setstate__` of BaseModel by
  * Support `PEP 746` by
  * Allow validator and serializer functions to have default values by
  * Fix bug with mypy plugin's handling of covariant `TypeVar` fields by
  * Fix multiple annotation / constraint application logic by
  * Respect `regex` flags in validation and json schema by
  * Fix type hint on `IpvAnyAddress` by
  * Allow a field specifier on `__pydantic_extra__` by
  * Use normalized case for file path comparison by
  * Modify constraint application logic to allow field constraints on `Optional[Decimal]` by
  * `validate_call` type params fix by
  * Check all warnings returned by pytest.warns() by
  * Reuse `re.Pattern` object in regex patterns to allow for regex flags by


### New Contributors[¶](https://docs.pydantic.dev/latest/changelog/#new-contributors_13)

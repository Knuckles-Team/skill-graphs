## v0.10.0 (2018-06-11)[¶](https://docs.pydantic.dev/latest/changelog/#v0100-2018-06-11)
  * add `Config.allow_population_by_alias`
  * **breaking change** : new errors format
  * **breaking change** : removed `Config.min_number_size` and `Config.max_number_size`
  * **breaking change** : correct behaviour of `lt` and `gt` arguments to `conint` etc. `le` and `ge`
  * added error context and ability to redefine error message templates using `Config.error_msg_templates`
  * fix typo in validator exception
  * copy defaults to model values, so different models don't share objects

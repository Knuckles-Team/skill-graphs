## v2.11.0a1 (2025-01-30)[¶](https://docs.pydantic.dev/latest/changelog/#v2110a1-2025-01-30)
### What's Changed[¶](https://docs.pydantic.dev/latest/changelog/#whats-changed_17)
Pydantic v2.11 is a version strongly focused on build time performance of Pydantic models (and core schema generation in general). This is an early alpha release, meant to collect early feedback from users having issues with core schema builds.
#### Packaging[¶](https://docs.pydantic.dev/latest/changelog/#packaging_9)
  * Bump dawidd6/action-download-artifact from 6 to 7 by
  * Re-enable memray related tests on Python 3.12+ by
  * Bump astral-sh/setup-uv to 5 by
  * Bump `ruff` to v0.9.0 by
  * Regular `uv.lock` deps update by
  * Add a `check_pydantic_core_version()` function by
  * Remove `greenlet` development dependency by
  * Bump `pydantic-core` to v2.28.0 by


#### New Features[¶](https://docs.pydantic.dev/latest/changelog/#new-features_6)
  * Support unsubstituted type variables with both a default and a bound or constraints by
  * Add a `default_factory_takes_validated_data` property to `FieldInfo` by
  * Raise a better error when a generic alias is used inside `type[]` by
  * Properly support PEP 695 generics syntax by
  * Properly support type variable defaults by


#### Changes[¶](https://docs.pydantic.dev/latest/changelog/#changes_4)
  * Rework `create_model` field definitions format by
  * Raise a deprecation warning when a field is annotated as final with a default value by
  * Deprecate accessing `model_fields` and `model_computed_fields` on instances by
  * Move core schema generation logic for path types inside the `GenerateSchema` class by
  * Move `deque` schema gen to `GenerateSchema` class by
  * Move `Mapping` schema gen to `GenerateSchema` to complete removal of `prepare_annotations_for_known_type` workaround by
  * Remove Python 3.8 Support by
  * Disable `pydantic-core` core schema validation by


#### Performance[¶](https://docs.pydantic.dev/latest/changelog/#performance_2)
  * Only evaluate `FieldInfo` annotations if required during schema building by
  * Optimize calls to `get_type_ref` by
  * Improve `__setattr__` performance of Pydantic models by caching setter functions by
  * Improve annotation application performance by
  * Improve performance of `_typing_extra` module by
  * Refactor and optimize schema cleaning logic by


#### Fixes[¶](https://docs.pydantic.dev/latest/changelog/#fixes_16)
  * Add validation tests for `_internal/_validators.py` by
  * Improve `TypeAdapter` instance repr by
  * Revert "ci: use locally built pydantic-core with debug symbols by
  * Re-enable all FastAPI tests by
  * Fix typo in HISTORY.md. by
  * Infer final fields with a default value as class variables in the mypy plugin by
  * Recursively unpack `Literal` values if using PEP 695 type aliases by
  * Override `__subclasscheck__` on `ModelMetaclass` to avoid memory leak and performance issues by
  * Remove unused `_extract_get_pydantic_json_schema()` parameter by
  * Add FastAPI and SQLModel to third-party tests by
  * Fix conditional expressions syntax for third-party tests by
  * Move FastAPI tests to third-party workflow by
  * Improve discriminated union error message for invalid union variants by
  * Unpack PEP 695 type aliases if using the `Annotated` form by
  * Include `openapi-python-client` check in issue creation for third-party failures, use `main` branch by
  * Add pandera third-party tests by
  * Add ODMantic third-party tests by
  * Add missing stacklevel in `deprecated_instance_property` warning by
  * Copy `WithJsonSchema` schema to avoid sharing mutated data by
  * Do not cache parametrized models when in the process of parametrizing another model by
  * Re-enable Beanie third-party tests by
  * Add discriminated union related metadata entries to the `CoreMetadata` definition by
  * Consolidate schema definitions logic in the `_Definitions` class by
  * Support initializing root model fields with values of the `root` type in the mypy plugin by
  * Fix various issues with dataclasses and `use_attribute_docstrings` by
  * Only compute normalized decimal places if necessary in `decimal_places_validator` by
  * Fix two misplaced sentences in validation errors documentation by
  * Fix mkdocstrings inventory example in documentation by
  * Add support for `validation_alias` in the mypy plugin by
  * Do not transform model serializer functions as class methods in the mypy plugin by
  * Simplify `GenerateJsonSchema.literal_schema()` implementation by
  * Add additional allowed schemes for `ClickHouseDsn` by
  * Coerce decimal constraints to `Decimal` instances by
  * Fix `ValueError` on year zero by


### New Contributors[¶](https://docs.pydantic.dev/latest/changelog/#new-contributors_8)

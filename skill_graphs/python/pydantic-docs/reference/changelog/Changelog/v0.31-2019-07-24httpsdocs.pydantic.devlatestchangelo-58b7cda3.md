## v0.31 (2019-07-24)[¶](https://docs.pydantic.dev/latest/changelog/#v031-2019-07-24)
  * better support for floating point `multiple_of` values,
  * fix schema generation for `NewType` and `Literal`,
  * fix `alias_generator` and field config conflict,
  * more detailed message for `EnumError`,
  * add advanced exclude support for `dict`, `json` and `copy`,
  * fix bug in `GenericModel` for models with concrete parameterized fields,
  * add documentation for `Literal` type,
  * add `Config.keep_untouched` for custom descriptors support,
  * use `inspect.cleandoc` internally to get model description,
  * add `Color` to schema generation, by
  * add documentation for Literal type,

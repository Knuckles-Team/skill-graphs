## v0.26 (2019-05-22)[¶](https://docs.pydantic.dev/latest/changelog/#v026-2019-05-22)
  * fix to schema generation for `IPvAnyAddress`, `IPvAnyInterface`, `IPvAnyNetwork`
  * fix variable length tuples support,
  * fix return type hint for `create_model`,
  * **Breaking Change:** fix `.dict(skip_keys=True)` skipping values set via alias (this involves changing `validate_model()` to always returns `Tuple[Dict[str, Any], Set[str], Optional[ValidationError]]`),
  * fix to schema generation for `IPv4Address`, `IPv6Address`, `IPv4Interface`, `IPv6Interface`, `IPv4Network`, `IPv6Network`
  * add `Color` type,

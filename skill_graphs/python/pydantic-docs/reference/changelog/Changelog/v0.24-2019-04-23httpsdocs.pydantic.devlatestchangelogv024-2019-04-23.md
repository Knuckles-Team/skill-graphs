## v0.24 (2019-04-23)[¶](https://docs.pydantic.dev/latest/changelog/#v024-2019-04-23)
  * fix handling `ForwardRef` in sub-types, like `Union`,
  * fix secret serialization,
  * Support custom validators for dataclasses,
  * fix `parse_obj` to cope with dict-like objects,
  * fix to schema generation in nested dataclass-based models,
  * fix `json` for `Path`, `FilePath`, and `DirectoryPath` objects,

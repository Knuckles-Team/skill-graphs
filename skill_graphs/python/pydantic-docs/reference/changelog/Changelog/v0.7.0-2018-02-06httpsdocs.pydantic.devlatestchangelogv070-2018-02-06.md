## v0.7.0 (2018-02-06)[¶](https://docs.pydantic.dev/latest/changelog/#v070-2018-02-06)
  * added compatibility with abstract base classes (ABCs)
  * add `create_model` method
  * **breaking change** : rename `.config` to `.__config__` on a model
  * **breaking change** : remove deprecated `.values()` on a model, use `.dict()` instead
  * remove use of `OrderedDict` and use simple dict
  * add `Config.use_enum_values`
  * add wildcard validators of the form `@validate('*')`

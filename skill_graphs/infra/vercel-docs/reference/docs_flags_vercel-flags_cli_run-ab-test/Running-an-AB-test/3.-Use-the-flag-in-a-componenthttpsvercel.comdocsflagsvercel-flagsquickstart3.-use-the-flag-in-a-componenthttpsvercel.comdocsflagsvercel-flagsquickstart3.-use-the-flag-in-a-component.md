##  [3. Use the flag in a component](https://vercel.com/docs/flags/vercel-flags/quickstart#3.-use-the-flag-in-a-component)[](https://vercel.com/docs/flags/vercel-flags/quickstart#3.-use-the-flag-in-a-component)
app/pricing/page.tsx
```
import { newPricingLayout } from '../../flags';

export default async function PricingPage() {
  const useNewLayout = await newPricingLayout();

  return useNewLayout ? <NewPricing /> : <CurrentPricing />;
}
```

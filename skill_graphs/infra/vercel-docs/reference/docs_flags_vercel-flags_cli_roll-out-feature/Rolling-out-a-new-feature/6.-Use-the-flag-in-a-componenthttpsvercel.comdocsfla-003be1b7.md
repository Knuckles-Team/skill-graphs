##  [6. Use the flag in a component](https://vercel.com/docs/flags/vercel-flags/cli/roll-out-feature#6.-use-the-flag-in-a-component)[](https://vercel.com/docs/flags/vercel-flags/cli/roll-out-feature#6.-use-the-flag-in-a-component)
app/checkout/page.tsx
```
import { redesignedCheckout } from '../../flags';

export default async function CheckoutPage() {
  const showRedesign = await redesignedCheckout();

  return showRedesign ? <NewCheckout /> : <OldCheckout />;
}
```
